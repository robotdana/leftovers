---
alias_method_callers:
  - define_negated_matcher
---
allowed:
  - diffable?
  - actual
rules:
  - method:
      - have_attributes
      - has_attributes?
      - receive_messages
    caller:
      - keyword: true
  - method:
    - receive
    - respond_to
  caller:
    - possition: 1
