rules:
  # this doesn't handle custom prefix, suffix, attribute
  - name: attr_encrypted
    definer:
      - argument: 1
    caller:
      - argument: 1
        add_prefix: encrypted_
      - argument: 1
        add_prefix: encrypted_
        add_suffix: _iv
      - argument:
          - encryptor
          - encrypt_method
          - decrypt_method
          - key
